__pycache__/
*.pyc
.DS_Store
.gstack/
.worktrees/
.superpowers/

# Local Netlify folder
.netlify

# Node / Astro
node_modules/
dist/
.astro/

# /design-sync build output (regenerable from src + .design-sync/)
ds-bundle/

# Never commit credentials (Google service-account keys / OAuth secrets).
# The events sync reads them from a GitHub secret in CI, or
# GOOGLE_APPLICATION_CREDENTIALS locally.
*service-account*.json
client_secret*.json
careful-granite-*.json
gcp-*key*.json
